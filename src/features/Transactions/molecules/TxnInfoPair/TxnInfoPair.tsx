import * as React from 'react';
import styled from 'styled-components';


export const TxnInfoPair = (props: any) => {
    return (
        <StyledRoot {...props}>
            
        </StyledRoot>
    )
}

const StyledRoot = styled.div``;
