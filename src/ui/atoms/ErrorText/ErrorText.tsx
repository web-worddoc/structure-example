import * as React from 'react';
import styled from 'styled-components';


type Props = {

}

export const ErrorText = ({...rest}: Props) => {
    return (
        <StyledRoot {...rest}>
            
        </StyledRoot>
    )
}

const StyledRoot = styled.div``;
